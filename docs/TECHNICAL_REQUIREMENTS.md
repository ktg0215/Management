# 技術要件・依存関係仕様書

**最終更新**: 2025年1月30日

## 目次

1. [技術スタック](#1-技術スタック)
2. [システム要件](#2-システム要件)
3. [依存関係](#3-依存関係)
4. [開発環境構築](#4-開発環境構築)
5. [ビルド・デプロイ](#5-ビルドデプロイ)
6. [パフォーマンス要件](#6-パフォーマンス要件)
7. [セキュリティ要件](#7-セキュリティ要件)
8. [ブラウザ対応](#8-ブラウザ対応)

---

## 1. 技術スタック

### 1.1 フロントエンド

| 技術/ライブラリ | バージョン | 用途 |
|----------------|-----------|------|
| **Next.js** | 15.3.5 | Reactフレームワーク（App Router） |
| **React** | 18.2.0 | UIライブラリ |
| **TypeScript** | 5.0+ | 型安全性 |
| **Tailwind CSS** | 3.4.0 | スタイリング |
| **Zustand** | 5.0.6 | 状態管理 |
| **React Query** | 5.85.5 | データフェッチング・キャッシング |
| **Lucide React** | 0.525.0 | アイコンライブラリ |
| **date-fns** | 4.1.0 | 日付操作 |
| **ExcelJS** | 4.4.0 | Excel出力 |
| **@dnd-kit** | 6.3.1 | ドラッグ&ドロップ |

### 1.2 バックエンド

| 技術/ライブラリ | バージョン | 用途 |
|----------------|-----------|------|
| **Node.js** | 18.x | ランタイム |
| **Express** | 4.18.2 | Webフレームワーク |
| **TypeScript** | 5.0.0 | 型安全性 |
| **PostgreSQL** | 15.x | データベース |
| **pg** | 8.10.0 | PostgreSQLクライアント |
| **bcryptjs** | 2.4.3 | パスワードハッシュ化 |
| **jsonwebtoken** | 9.0.0 | JWT認証 |
| **cors** | 2.8.5 | CORS設定 |
| **helmet** | 6.0.1 | セキュリティヘッダー |
| **morgan** | 1.10.0 | HTTPロガー |
| **dotenv** | 16.0.3 | 環境変数管理 |

### 1.3 開発ツール

| ツール | バージョン | 用途 |
|-------|-----------|------|
| **ESLint** | 9.x | コード品質チェック |
| **Playwright** | 1.54.2 | E2Eテスト |
| **Jest** | 29.7.0 | ユニットテスト |
| **Puppeteer** | 24.12.0 | ブラウザ自動化 |
| **Nodemon** | 2.0.22 | ホットリロード |

---

## 2. システム要件

### 2.1 開発環境

**必須**:
- **Node.js**: 18.0.0 以上
- **npm**: 9.0.0 以上
- **PostgreSQL**: 15.0 以上
- **Git**: 2.30 以上

**推奨**:
- **OS**: Windows 11 / macOS 13+ / Ubuntu 22.04 LTS
- **メモリ**: 8GB以上
- **ストレージ**: 10GB以上の空き容量

### 2.2 本番環境

**サーバースペック**:
- **CPU**: 2コア以上
- **メモリ**: 4GB以上
- **ストレージ**: 50GB以上
- **OS**: Ubuntu 22.04 LTS（推奨）

**ソフトウェア**:
- Node.js 18.x
- PostgreSQL 15.x
- Nginx 1.22+
- PM2（プロセス管理）
- Let's Encrypt（SSL証明書）

---

## 3. 依存関係

### 3.1 フロントエンド依存関係

#### プロダクション依存関係

```json
{
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "@tanstack/react-query": "^5.85.5",
    "@tanstack/react-query-devtools": "^5.85.5",
    "autoprefixer": "^10.4.21",
    "date-fns": "^4.1.0",
    "exceljs": "^4.4.0",
    "lucide-react": "^0.525.0",
    "next": "^15.3.5",
    "postcss": "^8.5.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tailwindcss": "^3.4.0",
    "uuid": "^11.1.0",
    "ws": "^8.18.3",
    "zustand": "^5.0.6"
  }
}
```

#### 開発依存関係

```json
{
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@testing-library/jest-dom": "^6.1.5",
    "@testing-library/react": "^14.1.2",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.3.4",
    "jest": "^29.7.0",
    "puppeteer": "^24.12.0",
    "typescript": "^5"
  }
}
```

### 3.2 バックエンド依存関係

#### プロダクション依存関係

```json
{
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^6.0.1",
    "ioredis": "^5.3.2",
    "jsonwebtoken": "^9.0.0",
    "morgan": "^1.10.0",
    "pg": "^8.10.0",
    "ws": "^8.14.2"
  }
}
```

#### 開発依存関係

```json
{
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/morgan": "^1.9.10",
    "@types/node": "^24.0.3",
    "@types/pg": "^8.15.4",
    "@types/ws": "^8.5.10",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
```

---

## 4. 開発環境構築

### 4.1 初回セットアップ

```bash
# 1. リポジトリクローン
git clone <repository-url>
cd project

# 2. フロントエンド依存関係インストール
cd next-app
npm install

# 3. バックエンド依存関係インストール
cd ../backend
npm install

# 4. PostgreSQL データベース作成
createdb management_system

# 5. 環境変数設定
# next-app/.env.local
echo "NEXT_PUBLIC_API_URL=http://localhost:3001" > .env.local

# backend/.env
cd ../backend
cat > .env << EOL
DATABASE_URL=postgresql://postgres:password@localhost:5432/management_system
JWT_SECRET=your-secret-key-change-this
PORT=3001
EOL
```

### 4.2 開発サーバー起動

**ターミナル1 - バックエンド**:
```bash
cd backend
npm run dev
# → http://localhost:3001 で起動
```

**ターミナル2 - フロントエンド**:
```bash
cd next-app
npm run dev
# → http://localhost:3002 で起動
```

### 4.3 データベースマイグレーション

```bash
cd backend
npm run migrate:pl
```

---

## 5. ビルド・デプロイ

### 5.1 フロントエンドビルド

```bash
cd next-app

# 型チェック
npm run type-check

# Lintチェック
npm run lint

# プロダクションビルド
npm run build

# ビルド成果物: .next/ ディレクトリ
```

### 5.2 バックエンドビルド

```bash
cd backend

# TypeScript コンパイル
npx tsc

# コンパイル成果物: dist/ ディレクトリ

# プロダクション起動
npm start
```

### 5.3 デプロイメント

**PM2を使用した本番デプロイ**:

```bash
# PM2 インストール
npm install -g pm2

# バックエンド起動
cd backend
pm2 start npm --name "management-api" -- start

# 自動起動設定
pm2 startup
pm2 save
```

**Nginxリバースプロキシ設定**:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # フロントエンド
    location / {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # バックエンドAPI
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 6. パフォーマンス要件

### 6.1 レスポンスタイム目標

| ページ種別 | 目標レスポンスタイム | 現在の実績 |
|-----------|---------------------|------------|
| 静的ページ | < 500ms | ~400ms |
| ダッシュボード | < 1000ms | ~800ms |
| データ取得ページ | < 1500ms | ~1200ms |
| データ保存処理 | < 2000ms | ~1500ms |

### 6.2 最適化手法

**フロントエンド**:
- ✅ React Query によるデータキャッシング
- ✅ Lazy Loading（SalesTable等の重いコンポーネント）
- ✅ useMemo/useCallback によるメモ化
- ✅ Next.js 自動コード分割
- ✅ 画像最適化（Next.js Image）

**バックエンド**:
- ✅ データベースインデックス最適化
- ✅ N+1クエリ問題の回避
- ✅ Compression ミドルウェア
- 🚧 Redis キャッシング（将来実装）

### 6.3 データベースパフォーマンス

**インデックス戦略**:
```sql
-- 頻繁にクエリされるカラム
CREATE INDEX idx_employees_store_id ON employees(store_id);
CREATE INDEX idx_stores_business_type_id ON stores(business_type_id);
CREATE INDEX idx_shift_submissions_employee_id ON shift_submissions(employee_id);
CREATE INDEX idx_shift_submissions_period_id ON shift_submissions(period_id);
CREATE INDEX idx_shift_entries_work_date ON shift_entries(work_date);
```

---

## 7. セキュリティ要件

### 7.1 認証・認可

**JWT設定**:
```javascript
{
  algorithm: 'HS256',
  expiresIn: '7d',
  issuer: 'management-system',
  audience: 'management-users'
}
```

**パスワードハッシュ化**:
```javascript
bcrypt.hash(password, saltRounds: 10)
```

### 7.2 通信セキュリティ

- ✅ HTTPS強制（本番環境）
- ✅ CORS設定（許可オリジンのみ）
- ✅ Helmet.jsによるセキュリティヘッダー
- ✅ CSRFトークン（SameSite Cookie）

### 7.3 データベースセキュリティ

- ✅ パラメータ化クエリ（SQLインジェクション対策）
- ✅ 最小権限原則（データベースユーザー）
- ✅ 機密情報の暗号化（パスワード）
- ✅ 接続文字列の環境変数化

### 7.4 入力バリデーション

**フロントエンド**:
```typescript
// React Hook Form + Zod
const schema = z.object({
  employeeId: z.string().min(1).max(20),
  password: z.string().min(8),
  email: z.string().email().optional()
});
```

**バックエンド**:
```javascript
// express-validator
body('employeeId').isLength({ min: 1, max: 20 }).trim().escape(),
body('password').isLength({ min: 8 }),
body('email').optional().isEmail().normalizeEmail()
```

---

## 8. ブラウザ対応

### 8.1 対応ブラウザ

| ブラウザ | 最小バージョン | テスト済み |
|---------|---------------|-----------|
| Chrome | 120+ | ✅ 128 |
| Safari | 17+ | ✅ 17.4 |
| Edge | 120+ | ✅ 128 |
| Firefox | 120+ | 🚧 未テスト |

**非対応**:
- Internet Explorer（全バージョン）

### 8.2 レスポンシブデザイン

**ブレークポイント**:
```css
/* Tailwind CSS デフォルト */
sm: 640px   /* モバイル横向き */
md: 768px   /* タブレット */
lg: 1024px  /* デスクトップ */
xl: 1280px  /* 大画面 */
```

**対応デバイス**:
- ✅ デスクトップ（1920x1080）
- ✅ ラップトップ（1366x768）
- ✅ タブレット（iPad、1024x768）
- ✅ スマートフォン（375x667 以上）

---

## 9. モニタリング・ログ

### 9.1 ログ設定

**バックエンドログ**:
```javascript
// Morgan ミドルウェア
app.use(morgan('combined', {
  stream: fs.createWriteStream('./logs/access.log', { flags: 'a' })
}));
```

**ログローテーション**:
```bash
# logrotate 設定
/var/log/management-system/*.log {
    daily
    rotate 30
    compress
    missingok
    notifempty
}
```

### 9.2 エラー追跡

**フロントエンド**:
- React ErrorBoundary
- Console エラーログ

**バックエンド**:
- Express エラーハンドリングミドルウェア
- ファイルログ出力
- 🚧 Sentry連携（将来実装）

---

## 10. テスト要件

### 10.1 テストカバレッジ目標

| テスト種別 | 目標カバレッジ | 現在の状況 |
|-----------|---------------|-----------|
| ユニットテスト | 80%+ | 🚧 未実装 |
| 統合テスト | 70%+ | 🚧 一部実装 |
| E2Eテスト | 主要フロー100% | 🚧 未実装 |

### 10.2 テストコマンド

```bash
# フロントエンド
cd next-app

# 型チェック
npm run type-check

# Lint
npm run lint

# ユニットテスト
npm run test

# E2Eテスト
npm run test:playwright

# カバレッジレポート
npm run test:coverage

# バックエンド
cd backend

# 型チェック
npx tsc --noEmit

# テスト実行
npm test
```

---

## 11. 環境変数一覧

### 11.1 フロントエンド環境変数

| 変数名 | 説明 | 必須 | デフォルト値 |
|-------|------|------|-------------|
| `NEXT_PUBLIC_API_URL` | バックエンドAPIのベースURL | ❌ | `http://localhost:3001` |
| `NODE_ENV` | 環境（development/production） | ✅ | `development` |

### 11.2 バックエンド環境変数

| 変数名 | 説明 | 必須 | デフォルト値 |
|-------|------|------|-------------|
| `DATABASE_URL` | PostgreSQL接続文字列 | ✅ | - |
| `JWT_SECRET` | JWT署名用シークレットキー | ✅ | - |
| `PORT` | サーバーポート番号 | ❌ | `3001` |
| `NODE_ENV` | 環境（development/production） | ✅ | `development` |
| `REDIS_URL` | Redis接続文字列（オプション） | ❌ | - |

---

**更新履歴**:
- 2025年1月30日: 初版作成、全技術要件記載
